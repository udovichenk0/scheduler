import { Event, Store } from 'effector';
export declare function interval<S extends unknown, F extends unknown>({ timeout, start, stop, leading, trailing, }: {
    timeout: number | Store<number>;
    start: Event<S>;
    stop?: Event<F>;
    leading?: boolean;
    trailing?: boolean;
}): {
    tick: Event<void>;
    isRunning: Store<boolean>;
};
